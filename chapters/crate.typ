#text(red)[
  kéne vmi world builder amivel be lehet állítani a paramétereit
  (gravitáció, solver steps, separation force)
  ```rust
  struct World {}

  struct RigidBodyRef {}
  struct StaticBodyRef {}
  ```
]
